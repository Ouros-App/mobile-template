# {{PROJECT_NAME}}

{{APP_LABEL}} base Android nativa em Kotlin + XML.

## Placeholders

- `{{PROJECT_NAME}}`
- `{{APP_LABEL}}`
- `{{PACKAGE_NAME}}`
- `{{PACKAGE_PATH}}`
- `{{APPLICATION_CLASS_NAME}}`

## Inicializacao

Use `scripts/init-template.sh` para substituir os placeholders e preparar um novo projeto a partir deste template.
Copie `local.properties.example` para `local.properties` antes de abrir o projeto no Android Studio.
